(local {: setup} (require :nvim-autopairs))

(setup {:disable_filetype ["TelescopePrompt"
                           "fennel"
                           "clojure"
                           "lisp"]})
